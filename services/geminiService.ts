// El servicio de IA ha sido deshabilitado y eliminado del proyecto.
// Se mantiene este archivo vacío para evitar errores de importación si alguna referencia residual existe.

export const sendMessageToGemini = async (message: string) => {
  console.warn("El servicio de IA ha sido eliminado. Mensaje no enviado:", message);
  return null;
};